# 更新日志

每个 `v*` 标签发布时，GitHub Release 的说明取自这里对应版本段落（`scripts/release-notes.mjs` 提取）。

## v1.4.3（未发布）

### 修复
- **macOS 划词复制判定升级为剪贴板序号**（`NSPasteboard.changeCount`）：此前按「读到的文本与旧剪贴板不同」判定复制是否发生，选中词恰好与旧剪贴板内容相同时会被误报「没读到选中文本」；序号判定无此盲区，与 Windows 行为对齐。

## v1.4.2 - 2026-09-17

### 修复
- **划词直加选中整句时分流失效**：句子从未真正进过小窗原句栏——模拟复制后剪贴板已被还原，小窗再读剪贴板只能读到旧内容，旧剪贴板里的单词甚至会被误当成待收词。句子改为经后端暂存交接（`take_quick_payload`），小窗取到就用、取不到才读剪贴板。
- **划词直加可能收错词**：目标应用响应慢或忽略 ⌘C 时，固定等待后读到的其实是用户旧剪贴板。Windows 改用剪贴板序号判定复制是否真的发生，macOS 比对内容变化；没等到新内容就明确报「没读到选中文本」，宁可不收也不错收。
- **划词直加会毁掉剪贴板里的截图**：剪贴板原是图片时，模拟复制后被文字覆盖且不再还原。现在连图片一起快照、事后还原。
- **热键注册失败静默全灭**：改键先注销全部再挂载，一旦新键被占用就两个热键全没了且无提示。现在失败时回落默认键；设置页改为「先注册成功再落库」，失败即还原，不再把注册不了的键存进设置。
- **回到主页就全量云同步**：主页每次重新挂载都触发一整趟 GitHub 拉取+合并+推回（近 1MB）。自动同步加 5 分钟节流（手动同步不受影响）。

### 变更
- **安全收紧**：`fs` 权限从全盘读写（`**`）收窄到 Token/Key 两个点文件，导出/导入改走 Rust 命令直写对话框选定的路径；`csp: null` 改为严格 CSP（脚本仅限本源、IPC 显式放行）。
- **词书标签主键改大小写不敏感**（迁移 v7）：同词异大小写不再产生重复标签（「已在：生词本 · 生词本」），存量重复行自动收敛。
- 小窗可见而主窗隐藏时，点 Dock 图标也会唤起主窗（原先只看「有无可见窗口」，此时点了没反应）。

## v1.4.1 - 2026-09-17

### 修复
- 云同步防重入：开应用自动同步、复习结束同步、手动同步重叠时合并为一次执行，不再因并发各读到旧数据而重复建 Gist 或互相覆盖；「接入已有云端」也会先等在途同步结束再切换目标。
- 同步合并保留词的原始收入时间：不再把旧卡伪装成「删除后重收」而绕过删除记录错误复活；删除墓碑按较晚时间为准，堵住旧卡复活窗口。
- 复习评分防连发：按住回车或快速连点不再重复记一次成绩（此前会产生重复复习记录、打乱 FSRS 调度）。
- 今日进度按词计数：忘记重试的词不再被算成两三次，进度也不会超过 100%；且跟随当前词书筛选。
- 查词清空输入时不再闪回旧结果；快速收词小窗快速输入后回车，不再误收上一个词。
- 词库列表在切换筛选/搜索时不再出现旧请求串台。

## v1.4.0 - 2026-09-17

### 后台运行
- 关闭窗口不再退出：浸词驻留系统托盘后台，划词直加、查词小窗热键随时可用；托盘左键单击唤起主窗，右键菜单可「显示浸词 / 快速收词 / 退出」。
- 再次点开应用图标会唤起已在后台运行的实例，不会再开第二个进程。

### 云同步
- 新增「接入已有云端」：换新设备或重装后，在设置里粘贴原来的 Gist 地址或 ID 即可接入同一份云端数据，避免误建新 Gist 造成两台设备数据分叉。

### 词库
- 词书目录支持整本移除：摘掉整本书的标签，只在这本书里的词连学习记录一并删除（云同步会在另一台设备上同样删除）；重新导入即恢复。
- 多本书共有的词移除某本时保留卡片，只是不再属于那本书。

### 练习
- 句子填空支持词形挖空：原句里是 went / goes / going 而词卡是 go 时照样挖空，填原形或句中词形都算对（不再把答案亮在句子里）。

### 查词
- 拼写纠错：彻底查不到时，从常用词里给编辑距离相近的候选——recieve → receive、acomodate → accommodate、definately → definitely。

## v1.3.2 - 2026-09-17

### 收词与词库修复
- 收词必定真正进「生词本」：修复单词已在其他词书（如词书导入过）时点「加入」静默失败的问题——之前只显示「已在库」，词却始终不进生词本。
- 词库列表改为最新收的排最前：新收的词一眼可见，不再沉在列表底部。
- 收词后词库页实时刷新（快速收词小窗、划词直加、查词页都算）；导入词书后列表也立即更新。
- 升级后自动修复：历史上因同步问题丢失词书标签的词会按主词书补回，之前「收了却看不见」的词重新出现。

### 数据与统计修复
- 云同步：一台设备删除、另一台重新收词后，同步不再丢失该词的全部词书标签。
- 每日新词限额：同一天多次练习不再超发（新卡首次复习 10 分钟后到期，复习第二次会让旧计数漏掉它）。
- 「今日复习次数」改为与库内时间格式一致的比较（此前在其他时区可能归零）。
- 快速收词小窗的释义预览不再把换行符原样显示。
- 设置页两个热键选成相同值时，界面不再显示未保存的值。
- 词书导入的词同步清理旧删除墓碑，与手动收词一致，避免下次同步被误删。

## v1.3.1 - 2026-09-17

### 练习节奏修复
- 评「忘记」的词不再整条序列重来：只重做失败的最后一轮，已经过了的模式不会再次出现。
- 各词的练习模式顺序随机错开（新词仍先教学）：同一种模式不再成串连排出现。

## v1.3.0 - 2026-09-17

### 收词与词库
- 查词页快速收词固定进「生词本」，不再跟随当前浏览的词书；展开面板仍可选其他词书收入。
- 词库页新增删除：每行「删除」按钮，二次确认后连词带学习记录、词书标签一并移除。
- 删除走墓碑机制：云同步会在另一台设备上移除同一个词，且不会在下次同步时「复活」；删错重新收词即可正常恢复。

## v1.2.0 - 2026-09-17

### 查词：中文查英文
- 查词页直接输入中文即可找到英文单词：苹果 → apple，高兴 → pleasure / glad / joy。
- 核心词义优先（释义开头命中的排前），再按常用度排序，结果不淹没在长尾释义里。

## v1.1.0 - 2026-09-16

### 查词：前缀联想
- 打两三个字母就能看到常用词：候选按常用度排序（词频 → 牛津/柯林斯 → 词长），不再按字母序淹没在生僻词里。
- 精确命中不再独占结果，置顶后继续展示相近词。
- 前缀候选太少时自动补「包含」匹配（限常用词），忘了单词开头也能找回来。
- 响应更快（防抖 200ms → 100ms），快速连续输入不会旧结果闪回。

### 练习：间隔式多轮
- 一个单词不再一口气做完所有练习：看完教学卡就过，其余轮次隔着其他词交替回来，符合间隔记忆规律。
- 中间轮次答错的词会更快回来（隔 2 张）；最终轮统一记 FSRS 成绩，中途有错的词「简单」档不再可用。
- 进度以词为单位显示（已完成 / 总词数），回顾轮在模式提示里标注「回顾」。
- 忘记的卡仍是本轮内整序列重来；完成屏错词重练不变。

## v1.0.0 - 2026-09-07

第一个正式版。

浸词是一个桌面背单词应用：FSRS-5 记忆调度，真实语境学习，单色纸墨的克制设计。Windows 与 macOS 双平台，数据完全属于你。

### 记忆与练习
- **FSRS-5 调度**：按你的记忆表现安排每次复习，追求稳定的知识保留率。
- **模式序列**：每个单词把设置里勾选的练习方式各过一遍，全部完成才记成绩；新词先教后练，忘记的卡本轮内换式重现，完成屏错词一键重练。
- **七种练习方式**：卡片自评、默写拼写、看词选义、看义选词、听音拼写、句子填空、组词成句。后两种基于你收词时的原句，凭语境作答；键盘与鼠标皆可完成全流程。

### 收词与内容
- **快速收词**：在任何应用里选中单词，全局热键直接入书（默认 Alt+Q），或呼出小窗补一句原句（默认 Alt+E）；支持词形还原，went 回到 go、puddles 回到 puddle。
- **内置词典**：安装包内置约 77 万词的 ECDICT，查词、释义、词形还原全部离线可用。
- **词书目录**：内置内容包按需一键导入，自动与现有词库去重。首发八本：四级、六级、考研、托福、雅思、GRE、学术词汇（AWL）、计算机科学词表。一词多书，也可自建词书。

### 同步与数据
- **双端同步**：学习数据经由你自己的 GitHub 私有 Gist 在两台设备间自动收敛，不经任何第三方服务器；Token 存本机。支持手动导出/导入作为兜底。
- **匿词同步**：可选拉取匿词（clingword）收藏，新词带原句流入生词本。

### 设计
- **「砚」**：单色纸墨，唯一的彩色是青瓷水色，只给进度水线、焦点与成功态。方向性转场、数字滚动、弹簧形变图标；内置思源宋体，释义按纸质词典的惯例以宋体排印，深浅双主题。

### 从 0.2 到 1.0
v0.2 单机核心与复习闭环；v0.3 双设备导出合并；v0.4 一词多书架构；v0.5 GitHub Gist 云同步；v0.7 / v0.8 全局热键收词与词形还原；v0.9 「砚」重设计；v0.10 安装包内置词典；v0.11 句子练习；v0.12 / v0.13 原句收词与教学卡、错词小结；v0.14 模式序列化；v0.16 词书目录。

## v0.16.0 - 2026-09-07

### 新增
- **词书目录 + 一键导入**：词库页右上角「+ 词书」打开内置目录，按需导入内容包；导入自动与现有词库去重（已在库的词只补词书标签），词与学习数据照常云同步。
- **目录首发 8 本**：四级 / 六级 / 考研 / 托福 / 雅思 / GRE（取自 ECDICT 标签，按 BNC 词频排序）+ **学术词汇**（Academic Word List，570 词族 3,069 词形）+ **计算机**（Computer Science Word List，451 术语）。外部两份词表均为 CC0 公有领域。
- 词书保持单层结构，不做子分类；需要子集时用自建词书自由圈选。

## v0.15.1 - 2026-09-07

### 变更
- **内置思源宋体**：安装包随附 Noto Serif SC（可变字重，25MB，SIL OFL 开源许可）。Windows 没有任何可用的系统中文衬线，上一版的衬线栈实际上只有页眉几个字生效——这次中文展示文字真正用上了宋体。
- **释义改宋体排**：复习揭晓、新词教学、默写/填空/组词的释义统一宋体——纸质词典的惯例，释义就该是宋体；快速收词小窗等小尺寸界面仍是黑体，保证小字清晰。

## v0.15.0 - 2026-09-07

### 变更（静态质感：中文排版与材质）
- **中文排版**：衬线栈补上 Windows 的中文字体（华文中宋/宋体依次回落），中英混排的展示文字在 Windows 上首次真正衬线；设置/统计/词库页眉改为宋体书眉（加大半号 + 宽字距），贴合「砚」的纸墨语言。
- **宣纸纹理**：全屏极淡噪点材质层（浅色 3%、深色 4.5%），纯静态、不接事件、不随内容滚动重绘；快速收词小窗是透明窗体，已排除以免涂出窗体轮廓。
- **主页水印**：今日有任务时，视觉中心衬一张巨幅「浸」字水印（约 4.5% 墨色），空状态维持原样。
- **选区颜色**：文字选中底色从系统蓝改为青瓷水色。

## v0.14.3 - 2026-09-07

### 新增
- **复习序列内「上一个」**：进入下一个模式后可以退回当前词的上一模式再看一遍（按钮或退格/左方向键）；该词一旦评分进入下一词即不可回退（记忆记录已写入）。

### 变更
- **转场方向跟随入口位置**：查词在右上角，保持从右侧滑入；设置/统计/词库在左上角，改为从左侧滑入、返回主页时从右侧滑入。

## v0.14.2 - 2026-09-07

### 优化
- **键盘双键等效**：翻面、确认「良好」等所有推进动作，空格与回车均可触发（打字输入框内空格仍是字符本身）；提示文案同步改为「空格 / Enter」。
- 「下一个模式」按钮更名为「下一个」。

## v0.14.1 - 2026-09-07

### 修复
- **纯鼠标可完成全流程**：新词教学卡新增「开始练习」按钮（此前只能空格/回车推进）；默写/听写/句子填空新增「提交」按钮（此前只能回车提交）。其余环节（翻面、四选一、组词成句、下一模式、评分、完成屏）本就支持点击。

## v0.14.0 - 2026-09-07

### 变更（学习循环：模式序列化）
- **每个单词把勾选的所有模式各过一遍**：进入练习时按固定顺序（卡片自评 → 默写 → 看词选义 → 看义选词 → 听音拼写 → 句子填空 → 组词成句，与设置页顺序一致）逐个模式完成，最后一个模式作答后才进入评分；答对一键「良好」，答错四档自评。设置里勾选哪些模式，就过哪些。
- 新词仍然先教后练，且跳过默写/听写（先认识后练）；无收词原句的卡自动跳过句子填空/组词成句；队列不足 4 个词时自动跳过四选一。
- 评「忘记」的卡隔几张后整条模式序列重来（不再是单题重现）。
- 顶部新增「模式 2/6 · 默写」进度提示；设置页「练习方式」说明同步更新。

## v0.13.2 - 2026-09-07

### 修复（macOS 兼容专项）
- **主窗口没有红绿灯**：`decorations: false` 在 macOS 上是彻底无边框（连系统红绿灯一起没了），而自绘控制键又只在 Windows 渲染——Mac 用户此前只能靠菜单 Cmd+Q 关闭。新增 `tauri.macos.conf.json` 平台配置，macOS 改为 `decorations: true` + `titleBarStyle: Overlay` + `hiddenTitle`，红绿灯叠在内容上层，外观不变。
- **首次启动词典释放失败**：内置词典释放在 Rust setup 里直接 `fs::copy`，macOS 全新机器上数据目录还不存在，拷贝报 "No such file or directory"，查词/词形还原全废。现在先建目录再释放。
- **Mac 本地无法构建安装包**：`bundle.targets` 写死 `["nsis"]`（仅 Windows），本地 `tauri build` 在 macOS 上出不了 dmg；改为 `all`（CI 的 `--bundles` 显式参数不受影响）。
- 修复 macOS 下 `build_quick` 的 `unused_mut` 编译警告（transparent 平台门化后 mut 未使用）。

### 修复（Mac 体验跟进）
- **热键文案两端分开**：设置页热键选项此前统一显示「Alt+E / Alt+Q」，macOS 上实际是 ⌥ Option 键。抽出 `lib/platform.ts`，Mac 显示「⌥ E（Option）/ ⌥ Q（Option）/ ⌃⇧ 空格」，Windows 仍显示 Alt/Ctrl；存储的热键值全端一致，不受影响。
- **快速收词小窗白底**：macOS 上小窗此前退化为不透明（怕引 macos-private-api），深色圆角卡片外露出一圈白色窗体背景。现在开启 `macos-private-api` 特性 + `macOSPrivateApi` 配置，macOS 小窗同样透明，圆角卡片干净浮在桌面上，与 Windows 表现一致。
- **设置页数据路径写死 Windows 格式**：底部版权行在 macOS 上也显示 `%APPDATA%\com.returndm.immerso`，改为按平台显示（Mac 为 `~/Library/Application Support/com.returndm.immerso`）。
- **匿词同步误报 forbidden path**：fs 权限范围补上 `$HOME/.neath-api-key` 显式条目，此前 `**` 通配不覆盖主目录点文件，未配置 Key 时设置页常驻一条 ✗ 报错。

### 修复（云同步卡死）
- **Gist Token 从未保存成功（卡同步的根因）**：Token 存 `~/.immerso-gh-token`，但 fs 权限 `**` 通配在 macOS 上不匹配主目录点文件，写入被静默拒绝；保存按钮又没有 try/catch，失败零反馈——看起来配置了，实际同步永远找不到 Token。权限补上 `$HOME/.immerso-gh-token` 显式条目，保存/清除按钮加上错误提示。
- **同步请求移到 Rust 侧（reqwest 直连），连接 15s / 总 120s 超时**：此前走 plugin-http，大备份（约 1MB JSON）会被序列化成 3 倍体积的字节数组过 WebView IPC，直连 GitHub 慢速上传时前端超时先触发——出现「服务端已建 Gist、前端却报错」的死循环：gist id 永远存不下来，每次重试都新建一个孤儿 Gist。现备份以原生字符串直达 reqwest；GitHub API 强制要求的 User-Agent 头也已补上（缺失会被 403 拒绝）。
- **匿词收的词没有词书标签**：匿词同步写卡时未维护 `deck_words`，词书列表看不到匿词收的词；已随写卡补上映射（主同步链路的同等问题已在 v0.13.1 处理）。

## v0.13.0 - 2026-09-07

### 新增（核心循环升级，参考不背单词/词灵的学习手感）
- **忘记的卡当场重现**：复习中评「忘记」的卡不再等下次排期，而是隔几张后自动回到本轮队列，携带最新记忆状态换一种练习方式再考，答对才真正过关。
- **新词教学卡**：新词第一次出现先「教」——词、发音、完整释义、收词原句，按空格开始练习；不再让新词裸考。
- **完成屏错词小结**：复习结束列出本轮忘记的词与释义，一键「再练一轮错词」；统计改为「本轮 N 词 · 重试 M 次」，进度条按去重后的真实进度走。

### 说明
- 调度算法保持 FSRS-5 不变——要补的是教学与重现的手感，不是调度。
- 「学练交替成组」（新词 5 个一组教完交替练）留待下一版。

## v0.12.0 - 2026-09-07

### 新增
- **快速收词支持原句**：收词小窗新增「原句（可选）」一栏，随词一起收进词书——收词时顺手存一句，句子练习的弹药就是这么攒的。
- **划词直加智能分流**：选中单词/短语按 Alt+Q 仍然直接入书；选中的是整句（≥3 个词）时改为呼出收词小窗，句子自动落进原句栏，补上目标词回车即收。

### 变更
- **转场升级**：视图切换从单一淡入改为方向性转场——前进从右侧滑入、返回从左侧滑入、进入复习「下潜聚焦」（上浮+微缩放），入场带一瞬的柔和模糊；尊重系统减动效设置。

## v0.11.0 - 2026-09-07

### 新增
- **句子练习（语境学习）**：两种新练习方式，作用于收词时带原句的卡（查词收词填了原句、或匿词同步带入的），无原句的卡自动回落到其他方式——
  - **句子填空**：原句挖掉目标词，凭上下文语境把词填回去，自动判分；
  - **组词成句**：把原句单词打乱，按语序点击拼回整句，全对自动记「良好」。
- 设置 → 练习方式里勾选启用；由于例句来自你收词时的原句，收词时多填原句，可练的句子就越多。

### 修复
- **安装包内置词典释放重做**：v0.10.0 的「直读安装目录」在 Windows 上存在 sqlx 连接串解析问题（会报 unknown query parameter），改为应用首次启动把包内词典释放到数据目录（带大小校验，升级自动换新）；查词、收词、词形还原恢复正常。

### 变更
- 查词小窗默认热键为 Alt+E（v0.10.0 起），划词直加为 Alt+Q。

## v0.10.1 - 2026-09-06

### 新增
- **词库无限滚动**：词库页滚动到底自动加载下一页（每页 200 条），万级词库全量可浏览，不再截断在前 200 条；切换词书 / 过滤 / 搜索会防抖重置并从头加载；列表尾部显示总数。

## v0.10.0 - 2026-09-06

### 新增
- **安装包内置词典**：ECDICT 全量 77 万词 + 词形还原表直接打进安装包（约 85MB 资源），装完即用，不再需要手动生成词典。应用直读安装目录里的词典文件，零拷贝。
- 查词小窗默认热键改为 **Alt+E**（Ctrl+Shift+Space 常被输入法切换占用，Alt+Q 保持划词直加不变），设置里仍可改。

### 变更
- macOS 安装包同样内置词典（DMG 体积相应增大）；词典数据来自 [ECDICT](https://github.com/skywind3000/ECDICT)（CC BY-NC 4.0）。

## v0.9.2 - 2026-09-06

### 修复
- 视图切换后内容横移：滚动条改为常驻占位（`scrollbar-gutter: stable`）并换成细样式，出现/消失不再挤动布局。

### 新增
- **数字滚动**：首页待复习数、统计页的今日完成/连续天数/累计次数，都会从 0 快速起滚、越滚越慢停在第数上（词灵式）。
- 首页左下角词书切换换成自绘弹层：向上展开、hairline 列表、悬停底色、当前项水色高亮、箭头随开合旋转——告别系统原生下拉的硬朗感。

## v0.9.1 - 2026-09-06

### 新增
- **动效层**：视图切换整页淡入上浮（0.32s）；勾选与完成标记弹入；墨色按钮按压缩放。
- **悬停反馈**：查词条目悬停出现墨色底 pill；词书/过滤页签与设置选项悬停时下划线生长；输入框悬停时底线加深；开关联动描边。

## v0.9.0 - 2026-09-06

### 变更
- **UI 重设计「砚」**：全站走向极简——单色纸墨（浅色宣纸 / 深色淡墨），唯一的彩色是青瓷水色，只出现在水线、焦点与成功态；实心按钮改用墨色。
- **去盒子**：设置、词库、统计、查词的卡片全部改为发丝线分行的列表；输入框只保留底部一条线；评分按钮去四色，间隔数字改等宽字体。
- **词形还原上屏**：查词页与收词小窗会显示词形归并提示（went → go）；假音标（与单词相同的占位音标）不再显示。
- **morphicons 微形变**（spring 物理形变图标）：设置页主题切换 日↔月、查词与词库的加入按钮 加↔勾——全站其余元素保持静止。
- 词库页词书徽标从彩色圆片改为纯文本（四级 · 六级 · 雅思），词书与过滤页签改为文字 + 水色下划线。

## v0.8.0 - 2026-09-06

### 新增
- **划词直加入书（匿词式）**：任意应用里选中单词按热键（默认 Alt+Q），浸词模拟一次复制（macOS 为 ⌘C，复制后还原你的剪贴板），把词直接收进上次用的词书，并弹系统通知确认——全程不弹窗。
- **词形还原**：内置 ECDICT 词形还原表（78,680 条变形→词基），选中/输入 puddles、ran、geese 这类变形词会自动解析到 puddle、run、goose 的释义并收词基；小窗显示「词形 puddles → puddle」提示。
- 查词小窗**输入即时查释义**：手动输入也随打随查（160ms 防抖），不再只解析呼出时带入的词。

### 修复
- 快速收词小窗后圈那圈多余的窗口阴影边框（关窗影 + 卡片铺满窗口）。

### 其他
- 设置页「快速收词」拆成两项：划词直加入书（Alt+Q）与查词小窗（Ctrl+Shift+Space），可分别换键或关闭；自动兼容旧版单热键设置。
- macOS 首次使用划词直加需在系统设置授予辅助功能权限（全局热键与模拟按键同一权限）。

## v0.7.0 - 2026-09-06

### 新增
- **全局热键快速收词**：在任意应用里选中单词按 Ctrl+C，再按热键（默认 Alt+Q）呼出浸词收词小窗——自动带入剪贴板里的词，即时显示音标与释义，回车收进所选词书，Esc 或点击别处关闭。窗口置顶、不占任务栏、位置随呼出居中。
- 设置页新增「快速收词热键」：可选 Alt+Q / Alt+E / Ctrl+Shift+Space / 关闭，并记住上次收词的词书。
- macOS 首次使用热键时，系统会请求辅助功能权限（未签名应用的常规提示）。

### 其他
- 建立本更新日志，Release 说明不再是一句话空壳。

## v0.6.0 - 2026-09-06

### 新增
- 词库页一词多书：每个词显示它所属的**全部**词书徽标，可把词加入其他词书；在词库页输入新名字即自建词书。
- 词库过滤与计数改走 deck_words 多对多表，「全部」视图按词去重计数。

## v0.5.0 - 2026-09-06

### 新增
- 云同步：数据同步到 GitHub 私有 Gist，打开应用与每次复习结束后自动推送；两台设备配同一 Token 即互相同步（拉取 → 合并 → 传回三步收敛）。
- 手动导出 / 导入降级为离线兜底。

### 修复
- Gist 内容超过 1MB 被截断时，改用 raw_url 兜底拉取。

## v0.4.0 - 2026-09-06

### 新增
- 一词多书架构：新增 deck_words 多对多表（迁移 v3），FSRS 记忆状态跟词走，不再绑定单一词库。
- 四本词书全量灌入：四级 3849 / 六级 5407 / 托福 6970 / 雅思 5038 词。
- 卡片淡入淡出延长至 0.5s。

### 修复
- 选择题答完点「继续」或按 Enter 无法进入下一张的卡死。
- 自评翻面后按 Enter 等同「良好」。

## v0.3.0 - 2026-09-06

### 新增
- 双设备数据同步：导出 / 导入数据包，合并规则为同一张卡取学习进度较新的一台，复习记录按「词 + 时间 + 评分」去重补插。
- 记住窗口位置与尺寸。

## v0.2.0 - 2026-09-06

### 新增
- 双平台发布流水线：push 标签自动构建 Windows 安装包（NSIS）与 macOS 通用 DMG，发布到 GitHub Release。
- macOS 红绿灯窗口按钮、跨平台字体栈与数据路径适配。

## 初始版本 - 2026-09-05 ~ 09-06（未打标签）

- ECDICT 77 万词离线词典与查词收词（可带原句）。
- FSRS-5 复习闭环：自评 / 默写 / 看词选义 / 看义选词 / 听音拼写五种练习，四档评分带间隔预览，每日新词配额。
- 词书分区（四级 / 六级 / 托福 / 雅思 / 考研 / GRE）、词库浏览页、统计页（进度环 / 热力图 / 连续天数）。
- 「水墨与纸」双主题（深色墨潭 / 浅色宣纸）、无边框沉浸窗口、卡片动效。
- 匿词（Neath）收藏只读同步。
